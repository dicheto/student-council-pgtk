# Администраторска Панел - Документация

## 📋 Преглед

Администраторската панел предоставя пълноценен интерфейс за управление на съдържанието на уебсайта, включително новини, събития, галерия и потребители.

## 🚀 Функционалности

### Dashboard
- **Статистики**: Преглед на общо новини, активни събития, регистрирани потребители и прегледи
- **Анимирани графики**: Визуализация на данни с Framer Motion
- **Последна активност**: Списък с последните действия в системата

### Управление на Новини

#### Multi-Step Форма
Формата за създаване на новини е разделена на 3 стъпки:

1. **Съдържание**
   - Заглавие на статията
   - Кратко описание
   - Пълно съдържание
   - **AI Content Generator**: Автоматично генериране на съдържание с OpenRouter (безплатни модели)

2. **Изображения**
   - Drag & Drop качване на множество изображения
   - Live preview галерия
   - Премахване на изображения

3. **Настройки**
   - Публикуване в Discord (Rich Embed)
   - Discord Webhook конфигурация
   - Тестване на webhook
   - Допълнителни опции за публикуване

## 🔧 Настройка

### OpenRouter API (Безплатни модели)

AI Content Generator използва **OpenRouter** с безплатни модели. Можете да използвате безплатно без API ключ, или да добавите ключ за по-добри резултати:

1. Създайте `.env.local` файл в root директорията (опционално)
2. Добавете (опционално, за по-добри модели):
```env
OPENROUTER_API_KEY=your_openrouter_api_key_here
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

**Безплатни модели:**
- Llama 3.2 3B (Meta) - по подразбиране
- Gemma 2 2B (Google)
- Phi-3 Mini (Microsoft)
- Qwen 2 1.5B (Alibaba)

Можете да изберете модел от dropdown менюто преди генериране.

### Discord Webhook

За публикуване в Discord:

1. Създайте webhook в Discord канала:
   - Отидете в Channel Settings → Integrations → Webhooks
   - Създайте нов webhook
   - Копирайте webhook URL

2. Можете да го зададете:
   - Глобално в `.env.local`: `DISCORD_WEBHOOK_URL=your_webhook_url`
   - Или за всяка статия индивидуално в формата

## 📁 Структура

```
app/admin/
├── layout.tsx              # Admin layout с навигация
├── page.tsx                 # Dashboard страница
└── news/
    └── page.tsx            # News Management страница

components/admin/
├── StatsCard.tsx           # Статистически карти
├── ChartCard.tsx            # Графики
├── RecentActivity.tsx       # Последна активност
└── news/
    ├── NewsList.tsx         # Списък с новини
    ├── NewsFormModal.tsx    # Модална форма
    ├── NewsForm.tsx         # Multi-step форма
    ├── AIContentGenerator.tsx # AI генератор
    ├── ImageUpload.tsx      # Image upload компонент
    └── DiscordToggle.tsx    # Discord интеграция

app/api/
├── ai/
│   └── generate-content/    # OpenAI API endpoint
└── discord/
    ├── test-webhook/        # Тест на webhook
    └── publish/             # Публикуване в Discord
```

## 🎨 Особености

### Анимации
- Плавни преходи между стъпките на формата
- Анимирани графики и статистики
- Hover ефекти на всички интерактивни елементи

### Responsive Design
- Пълно адаптивен дизайн
- Mobile-friendly навигация
- Оптимизиран за всички екрани

### Безопасност
- Защитени API routes
- Валидация на входни данни
- Error handling

## 🔐 Защита на Routes

В момента администраторската панел не е защитена. За production трябва да добавите:

1. Автентификация (Supabase Auth)
2. Role-based access control
3. Middleware за проверка на права

Пример:
```typescript
// middleware.ts
export function middleware(request: NextRequest) {
  // Проверка за автентификация и роля
  if (request.nextUrl.pathname.startsWith('/admin')) {
    // Проверка за admin роля
  }
}
```

## 📝 Използване

### Създаване на нова статия

1. Отидете в `/admin/news`
2. Кликнете на "Нова Статия"
3. Попълнете заглавието
4. (Опционално) Кликнете на "AI Генератор" за автоматично генериране
5. Качете изображения (ако има)
6. Активирайте Discord публикуване (ако желаете)
7. Публикувайте

### AI Content Generator

AI генераторът използва **OpenRouter** с безплатни модели за създаване на пълна статия базирана на заглавието. Статията е:
- На български език
- Дълга поне 500 думи
- Добре структурирана
- Подходяща за ученически съвет

**Налични безплатни модели:**
- **Llama 3.2 3B** (Meta) - по подразбиране, бърз и ефективен
- **Gemma 2 2B** (Google) - добър за творческо писане
- **Phi-3 Mini** (Microsoft) - балансиран между скорост и качество
- **Qwen 2 1.5B** (Alibaba) - компактен и бърз

Можете да изберете модел от dropdown менюто преди генериране.

### Discord Rich Embed

Когато статията се публикува в Discord, тя се изпраща като Rich Embed с:
- Заглавие
- Описание
- Цвят (Primary blue)
- Timestamp
- Footer с брандинг
- Главно изображение (ако има)

## 🐛 Troubleshooting

### AI генераторът не работи
- OpenRouter работи безплатно без API ключ по подразбиране
- Ако имате проблеми, опитайте друг модел от dropdown менюто
- За по-добри резултати, можете да добавите `OPENROUTER_API_KEY` в `.env.local` (опционално)
- Проверете интернет връзката си

### Discord Webhook не работи
- Проверете дали webhook URL е правилен
- Използвайте "Тест" бутона за проверка
- Уверете се, че webhook не е изтрит в Discord

### Изображения не се качват
- Проверете размера на файловете (макс. 10MB)
- Уверете се, че форматът е поддържан (PNG, JPG, GIF, WEBP)

## 🔄 Следващи стъпки

- [ ] Интеграция с Supabase за база данни
- [ ] Image upload към Supabase Storage
- [ ] Автентификация и авторизация
- [ ] Редактиране на съществуващи статии
- [ ] Bulk операции
- [ ] Export/Import функционалност
