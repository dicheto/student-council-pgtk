# 🔗 Как да създадеш Discord Webhook

## Стъпка по стъпка инструкции

### Стъпка 1: Отвори Discord
1. Отвори Discord приложението (desktop, web или mobile)
2. Влез в сървъра където искаш да изпращаш съобщения

### Стъпка 2: Отиди в канала
1. Избери канала където искаш webhook-ът да изпраща съобщения
2. Кликни на **⚙️ Settings** (иконата до името на канала, горе вляво)

### Стъпка 3: Отвори Integrations
1. В менюто наляво, отиди в секцията **"Integrations"**
2. Кликни на **"Webhooks"**

### Стъпка 4: Създай нов Webhook
1. Кликни на бутона **"New Webhook"** (или "Create Webhook")
2. Ще се отвори прозорец с настройки

### Стъпка 5: Конфигурирай Webhook
1. **Име**: Въведи име (напр. "Student Council", "News Bot", и т.н.)
2. **Аватар** (опционално): Можеш да качиш снимка за webhook-а
3. **Канал**: Уверете се, че е избран правилният канал

### Стъпка 6: Копирай Webhook URL
1. Скрол надолу в прозореца
2. Намери секцията **"Webhook URL"**
3. Кликни на бутона **"Copy Webhook URL"** (или копирай ръчно)
4. ⚠️ **ВАЖНО**: Запази този URL на сигурно място!

### Стъпка 7: Запази Webhook
1. Кликни **"Save"** или **"Save Changes"**
2. Webhook-ът е готов за използване!

## 📋 Как изглежда Webhook URL

Webhook URL-ът изглежда така:
```
https://discord.com/api/webhooks/123456789012345678/AbCdEfGhIjKlMnOpQrStUvWxYz1234567890
```

Или по-кратък вариант:
```
https://discord.com/api/webhooks/WEBHOOK_ID/WEBHOOK_TOKEN
```

## ✅ Добави в .env.local

Отвори `.env.local` файла и добави:

```env
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/твоят_webhook_url_тук
```

## 🧪 Тестване на Webhook

### Вариант 1: От Admin панела
1. Отиди на `/admin/discord`
2. В секцията "Webhook Settings"
3. Въведи webhook URL-а
4. Кликни "Тест"
5. Ако е успешно, ще видиш съобщение в Discord канала!

### Вариант 2: Ръчно тестване
Можеш да тестваш webhook-а директно с curl:

```bash
curl -X POST "твоят_webhook_url" \
  -H "Content-Type: application/json" \
  -d '{"content": "Тестово съобщение!"}'
```

Или използвай онлайн инструменти като:
- https://webhook.site
- https://discord.com/api/webhooks/...

## 🔐 Безопасност

⚠️ **ВАЖНО**:
- **Никога не споделяй webhook URL публично!**
- Всеки с URL-а може да изпраща съобщения в канала
- Ако случайно споделиш URL-а, веднага го изтрий и създай нов
- Не комитирай `.env.local` в git

## 🗑️ Как да изтриеш Webhook

Ако искаш да изтриеш webhook:
1. Отиди в същия канал → Settings → Integrations → Webhooks
2. Намери webhook-а който искаш да изтриеш
3. Кликни на него
4. Скрол надолу и кликни **"Delete Webhook"**
5. Потвърди изтриването

## 🔄 Как да промениш Webhook

Ако искаш да промениш настройките:
1. Отиди в Settings → Integrations → Webhooks
2. Кликни на webhook-а
3. Промени настройките (име, аватар, канал)
4. Кликни **"Save Changes"**

## 📸 Визуална помощ

Ако не виждаш опциите:
- Уверете се, че имаш права "Manage Webhooks" в сървъра
- Отиди в Server Settings → Roles → провери правата си
- Ако нямаш права, помоли администратора на сървъра

## 🐛 Често срещани проблеми

### "Webhook не работи"
- Провери дали URL-ът е правилен (без интервали)
- Уверете се, че webhook-ът не е изтрит
- Провери дали каналът все още съществува

### "Не мога да видя Webhooks опцията"
- Уверете се, че имаш права "Manage Webhooks"
- Отиди в Server Settings → Roles и провери правата

### "Webhook изпраща съобщения, но не виждам името"
- В настройките на webhook, промени името
- Добави аватар за по-добро разпознаване

## 💡 Съвети

- Създай отделен webhook за всеки тип съобщение (новини, събития, и т.н.)
- Използвай описателни имена за по-лесно управление
- Регулярно проверявай дали webhook-ите все още работят
