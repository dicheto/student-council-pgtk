# 🚀 Ръководство за стартиране

## Стъпка 1: Инсталиране на зависимости

Ако още не са инсталирани зависимостите, отвори терминал в папката на проекта и изпълни:

```bash
npm install
```

## Стъпка 2: Настройка на Environment Variables

Създаден е файл `.env.local` в root директорията на проекта. Той съдържа:

```env
# OpenRouter API Key (Опционално)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# App URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Discord Webhook (Опционално)
DISCORD_WEBHOOK_URL=your_discord_webhook_url_here
```

### ⚠️ Важно:
- **OpenRouter API ключ е ОПЦИОНАЛЕН** - системата работи безплатно без него!
- Ако искаш по-добри резултати, можеш да получиш безплатен ключ от: https://openrouter.ai/keys
- Просто отвори `.env.local` и добави ключа (ако имаш)

## Стъпка 3: Стартиране на Development сървъра

В терминала изпълни:

```bash
npm run dev
```

Или ако използваш yarn:

```bash
yarn dev
```

## Стъпка 4: Отваряне в браузър

След като сървърът се стартира, ще видиш съобщение като:

```
  ▲ Next.js 14.2.0
  - Local:        http://localhost:3000
  - ready started server on 0.0.0.0:3000
```

Отвори браузъра и отиди на:
- **Главна страница**: http://localhost:3000
- **Admin панел**: http://localhost:3000/admin

## 📝 Какво да правиш след това?

### За да използваш AI генератора:

1. Отиди на `/admin/news`
2. Кликни "Нова Статия"
3. Въведи заглавие
4. Кликни "AI Генератор" - ще работи безплатно!

### За Discord интеграция:

1. Създай Discord webhook:
   - Отиди в Discord канала
   - Settings → Integrations → Webhooks
   - Създай нов webhook
   - Копирай URL-а

2. В формата за новини:
   - Активирай "Публикуване в Discord"
   - Въведи webhook URL
   - Кликни "Тест" за проверка

## 🐛 Често срещани проблеми

### Порт 3000 е зает
Ако порт 3000 е зает, Next.js автоматично ще използва следващия свободен порт (3001, 3002, и т.н.)

### Грешка при инсталация
```bash
# Изтрий node_modules и package-lock.json
rm -rf node_modules package-lock.json

# Инсталирай отново
npm install
```

### AI генераторът не работи
- Провери интернет връзката
- Опитай друг модел от dropdown менюто
- Провери конзолата за грешки (F12 в браузъра)

## 📚 Полезни команди

```bash
# Development сървър
npm run dev

# Build за production
npm run build

# Стартиране на production build
npm start

# Проверка за грешки
npm run lint
```

## ✅ Готово!

Сега можеш да започнеш да използваш уебсайта! 🎉
